# Status

**Status:** active, 2026-09-22

The maintained line of `tz_world` and the source of the hex package. GitHub still records the repository as a fork of `kimlai/tz_world`, which this file overrides for the dashboard.
